require 'spec_helper'

describe Dingo::Postgresql::Clusterdata::Backup do
  it 'has a version number' do
    expect(Dingo::Postgresql::Clusterdata::Backup::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
